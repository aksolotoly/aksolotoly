{
  "name": "test-qa",
  "version": "1.0.0",
  "description": "test for fabrique",
  "main": "cypress.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vika",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.16.0"
  }
}
